---
import DocsLayout from '../../../layouts/DocsLayout.astro'
import StatusPill from '../../../components/docs/StatusPill.astro'
---
<DocsLayout
  section="reference"
  href="/docs/reference/manifest/"
  title="Manifest reference"
  description="The payload manifest: IDENTITY, PROVIDES, DEPENDS, kinds, needs, checks — plus the L2 package manifest block."
>
  <div class="mt-16 max-w-2xl">
    <StatusPill status="planned" note="wave 3" />
    <h2 class="!mt-6">Not written yet — here is the plan.</h2>
    <p class="mt-3 text-lg leading-relaxed text-[var(--tb-c-text-2)]">The payload manifest: IDENTITY, PROVIDES, DEPENDS, kinds, needs, checks — plus the L2 package manifest block.</p>
    <p class="mt-4 leading-relaxed text-[var(--tb-c-text-2)]">
      This page lands with the reference wave. Until then, the
      <a href="/docs/architecture/">architecture section</a> covers the same surfaces at
      narrative depth, and the
      <a href="https://github.com/tamatebako/tebako">product repository</a> carries the
      normative specs.
    </p>
  </div>
</DocsLayout>
