---
layout: post
title:  "Tebako v2: Ruby calls Java calls Python (3 of 5)"
date:   2026-09-03 00:00:00 +0800
categories:
  - tebako
  - architecture
  - metanorma
  - java
author:
  name: The Tebako team
  email: open.source@ribose.com
  social_links:
    - https://github.com/tamatebako
excerpt: >-
  Post 3 of 5. Metanorma's PDF pipeline spawns a JVM from inside a
  packaged Ruby process. In tebako v2.1 that JVM is a spawned runtime —
  a store-resident child dispatched through tebako, never a co-mounted
  VFS java, never a host-PATH accident. Here is the whole composition.
---

= Tebako v2: Ruby calls Java calls Python (3 of 5)

[NOTE]
====
Post 3 of 5 in the series *Tebako v2*.

. link:/blog/2026-09-01-tebako-v2-stable-metanorma/[Tebako v2: One download, any document]
. link:/blog/2026-09-02-tebako-v2-anatomy-tpkg/[Tebako v2: Anatomy of a package]
. *Tebako v2: Ruby calls Java calls Python* (this post)
. link:/blog/2026-09-04-tebako-v2-jail-host-routing/[Tebako v2: The jail and the host]
. link:/blog/2026-09-05-tebako-v2-developer-story-benchmarks/[Tebako v2: Build your own]
====

Post 1's compile exercised four technology stacks. This post is the
wiring diagram: how a packaged Ruby process legally spawns a JVM it
cannot see on any host `PATH`, why Inkscape travels as a mounted payload
instead, and why Python is — for now — a deliberate host tool.


++++
<figure role="img" aria-label="Diagram: one Ruby process reaching Java, Inkscape, and Python through three declared mechanisms">
<svg viewBox="0 0 840 330" style="width:100%;height:auto;font-family:var(--font-mono,monospace);" xmlns="http://www.w3.org/2000/svg">
  <title>Ruby calls Java calls Python</title>
  <defs>
    <marker id="f3" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" style="fill:var(--tb-c-text-3,#96abb8)"/></marker>
    <marker id="f3a" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" style="fill:var(--tb-c-accent,#c36242)"/></marker>
  </defs>
  <rect x="40" y="108" width="230" height="96" rx="9" style="fill:none;stroke:var(--tb-c-accent,#c36242);stroke-width:1.5;stroke-width:2"/>
  <text x="155" y="146" text-anchor="middle" style="font-size:13px;fill:var(--tb-c-text-1,#1a2b34)">your packaged</text>
  <text x="155" y="164" text-anchor="middle" style="font-size:13px;fill:var(--tb-c-text-1,#1a2b34)">Ruby process</text>
  <text x="155" y="186" text-anchor="middle" style="font-size:10px;fill:var(--tb-c-text-3,#7a8b94)">metanorma compile</text>
  <line x1="270" y1="156" x2="324" y2="60" style="stroke:var(--tb-c-accent,#c36242);stroke-width:1.5" marker-end="url(#f3a)"/>
  <rect x="330" y="26" width="470" height="66" rx="9" style="fill:none;stroke:var(--tb-c-accent,#c36242);stroke-width:1.5"/>
  <text x="565" y="50" text-anchor="middle" style="font-size:12px;fill:var(--tb-c-text-1,#1a2b34)">Java — a spawned runtime</text>
  <text x="565" y="67" text-anchor="middle" style="font-size:9.5px;fill:var(--tb-c-text-2,#4a5b66)">the JVM exe + its env image are materialized</text>
  <text x="565" y="81" text-anchor="middle" style="font-size:9.5px;fill:var(--tb-c-text-2,#4a5b66)">and started under the same mounts (spec 30)</text>
  <line x1="270" y1="156" x2="324" y2="156" style="stroke:var(--tb-c-accent,#c36242);stroke-width:1.5" marker-end="url(#f3a)"/>
  <rect x="330" y="122" width="470" height="66" rx="9" style="fill:none;stroke:var(--tb-c-text-3,#96abb8);stroke-width:1.5"/>
  <text x="565" y="146" text-anchor="middle" style="font-size:12px;fill:var(--tb-c-text-1,#1a2b34)">Inkscape — a mounted payload</text>
  <text x="565" y="163" text-anchor="middle" style="font-size:9.5px;fill:var(--tb-c-text-2,#4a5b66)">one .tfs image mounted at /opt/inkscape,</text>
  <text x="565" y="177" text-anchor="middle" style="font-size:9.5px;fill:var(--tb-c-text-2,#4a5b66)">its files read through the VFS like local ones</text>
  <line x1="270" y1="156" x2="324" y2="252" style="stroke:f3;stroke-width:1.5" marker-end="url(#f3a)"/>
  <rect x="330" y="218" width="470" height="66" rx="9" style="fill:none;stroke:var(--tb-c-text-3,#96abb8);stroke-width:1.5" stroke-dasharray="5 4"/>
  <text x="565" y="242" text-anchor="middle" style="font-size:12px;fill:var(--tb-c-text-1,#1a2b34)">Python — a host tool</text>
  <text x="565" y="259" text-anchor="middle" style="font-size:9.5px;fill:var(--tb-c-text-2,#4a5b66)">the honest route: declared in the manifest as a need,</text>
  <text x="565" y="273" text-anchor="middle" style="font-size:9.5px;fill:var(--tb-c-text-2,#4a5b66)">used from the host, visible in the trace</text>
  <text x="420" y="316" text-anchor="middle" style="font-size:10.5px;fill:var(--tb-c-text-3,#7a8b94)">three mechanisms, one rule: everything the process uses is declared</text>
</svg>
</figure>
++++

_Figure 1 — Three routes out of one packaged process: a spawned runtime, a mounted payload, and a declared host tool._

== The composition, declared

// source: tebako-packages/metanorma manifests/payload.yaml (the
// language + toolkit edges) and #35 (the java runtime edge, java/05);
// docs/spec/30 §1 (the edge grammar); docs/spec/23 §10-11.
Everything below is *declared* — in the payload's in-image manifest,
not discovered at run time. The Metanorma payload's dependency set, in
the manifest's own grammar:

[source,yaml]
----
requires:
  # the runtime this payload boots on
  - kind: language
    engine: ruby
    constraint: "~> 3.3.0"
  # the XSL-FO converter (mn2pdf) and the validator (jing) are java
  - kind: runtime
    engine: java
    implementation: temurin
    constraint: ">= 21, < 26"
    expose: [java]          # the command names this payload surfaces
  # figure rasterization is a mounted toolkit payload
  - kind: toolkit           # isodoc rasterizes SVG figures through
    name: inkscape          # vectory -> inkscape for doc/html output
    constraint: ">= 1.3"
    mount: /opt/inkscape
----

Three different answers to "I need another technology," each chosen on
purpose:

* **ruby** — the runtime the package boots on (two artifacts, mounted,
  shared machine-wide; post 2).
* **java** — a *spawned runtime*: a separate child process, dispatched
  through tebako from the machine store.
* **inkscape** — a *co-mounted payload slice*: bytes in the same
  process, mounted at `/opt/inkscape`, dispatched longest-prefix.
* **python** (xml2rfc, IETF flavors) — a *host tool*: used when the
  host provides it, declared as such, journaled when it runs.

== Java: the spawned runtime (spec 30)

// source: docs/spec/30-runtime-spawned-dependency.md (NORMATIVE,
// implemented 2026-08-31, tebako#508); docs/spec/29 (the wrapper-exe
// pattern); PROGRESS/30 (the implementation record).
The java story is the newest piece of the architecture, and it is worth
stating precisely, because the first shape was different: the early v2
Metanorma builds co-mounted a JRE toolkit slice at `/opt/openjdk`. That
works for library-layer cases — but a VFS mount is un-exec'able by a
subprocess: `execve` resolves *host* paths, and a mount that lives
inside the parent's address space is invisible to the kernel. The
shipped answer is spec 30: **a runtime as a spawned dependency**.

The OpenJDK runtime is itself the second new pattern, spec 29's
*wrapper exe*. Tebako does not compile Temurin; it ships upstream bytes.
So the runtime's executable artifact is a small tebako-owned Rust
binary embedding the driver, and the environment image holds the real
JRE. The wrapper speaks the exact runtime-driver wire protocol, mounts
its own env image, and execs the interpreter the image's manifest
declares (`layout.interpreter: /bin/java`). On POSIX the interpreter
*replaces* the wrapper process; on Windows, where `exec` does not
exist, the wrapper spawns it and propagates the exit code verbatim.

When packaged Ruby code calls `java` — mn2pdf joining an XSL-FO
command line, jing validating against a schema — this happens:

// source: docs/spec/30 §2; ruby#106 (the tfs_spawn_prepare FFI head,
// merged 2026-09-01); tebako#508.
. The patched Ruby's spawn interception recognizes `java` as a name the
  payload's manifest *exposes*, and asks the in-process driver for a
  spawn plan through a C ABI call. An undeclared command name is never
  rewritten — it falls to the host `PATH`, journaled as a host spawn.
. The driver's plan is a full dispatch of the depended runtime's
  wrapper executable *from the store* — a host path, kernel-visible by
  construction: the store wrapper, the parent's shared mounts carried
  as `--tebako-image` triples, and `--tebako-entry java` plus the
  user's arguments.
. Arguments that name files under the parent's mounts are carried by
  *re-mounting the same image in the child at the same mount point* —
  the argument strings pass through unrewritten. (On Windows, where a
  child cannot serve in-VFS reads, the file is materialized to the
  exec cache and the argument rewritten to the host path — the shipped
  ruby-windows behavior, now the general rule for that visibility
  class.)
. The child's environment is scrubbed: the parent's entire
  runtime-injection surface (mount table, preload shim, jail spec,
  spawn lock) is deleted, and the child gets its own
  `TEBAKO_RUNTIME_IMAGE` pointing at the JRE env image. A foreign
  preload in the child would bind the parent's symbols — the strip is
  not optional.

Resolution is split between the actors that can each do their half: the
dispatching loader resolves the edge up front — the *shim* for a
managed command, the *bootstrap* for a standalone package (that half
landed in v2.1.6 — the story below) — download permitted, like the
primary runtime, and exports the pins as `TEBAKO_SPAWN_LOCK`
(`java=21.0.12:2.1.5`). The *driver* resolves cache-only at spawn time
and never downloads: a locked runtime gone from the store is a named
error, never a silent re-pick.

// source: docs/spec/30 §3 (the PATH launcher tier, amended 2026-09-03);
// spec 22 §3.1 (shell-form argument bridging, locked 2026-09-03);
// ruby#107; PROGRESS/19 (windows option (b)).
There is a second surface: in a shell-form spawn
(`system("java -jar …")`) the shell is the exec target, so the *command*
token resolves through `PATH`. For those, the parent's boot plants one
generated *launcher* per exposed name in a PATH-front directory — a
self-contained script that execs the planned child. A name that cannot
resolve bakes a fail-closed stub that prints the named error and exits
69. The command's *operands* bridge separately, parent-side, in the
interpreter's spawn hook: the command line is tokenized on unquoted
blanks (both quote characters honored), and any token naming an
embedded path is materialized to its host twin and rewritten —
byte-identical when nothing bridges. On POSIX the command token itself
is never planned: the shell keeps the string, so redirections, pipes,
and `&&` keep their semantics, and the launcher tier resolves the
command. Windows has no launcher tier; there the shell form's
first-token command is planned inline through the same plan FFI, and
the string is rebuilt as the quoted store executable plus the plan argv
plus the bridged remainder, `cmd.exe`'s separators and redirections
preserved.

And the rule that makes all of this worth the machinery: **a
depended-runtime spawn that cannot resolve through tebako fails with a
named error — it never falls through to a system java**. Falling
through to whatever happens to be on the host is exactly the
host-fragility bug class this spec exists to kill.

=== The last red leg: the jar the JVM could not see

// source: PROGRESS/32-standalone-spawn-dispatch.md (the full root-cause
// record); packed-mn#254 acceptance runs 33689243863 and 33724253024;
// tebako#519/#521 (v2.1.6/v2.1.7); tamatebako/ruby#107 (source
// v0.2.33); spec 22 §3.1 (locked 2026-09-03); tebako-packages/metanorma
// dogfood runs 33686160163/33731076066 (managed, green on 1.16.9-8).
None of this arrived in one piece, and the way the last gap was found
is worth telling, because each layer failed *loudly, by name* — which
is what made the next layer findable. Three times the same compile
died at the same step — jing's RELAX NG validation — and each death
was a different layer of the stack:

. **The toolkit era.** The first v2 Metanorma packages co-mounted a JRE
  slice, and a managed-mode compile died with `Unable to access jarfile
  /lib/ruby/gems/…/jing-20091111.jar`: a plain host JVM has no business
  reading a file that lives inside a mounted image. That failure class
  is exactly why the toolkit form was retired and the java edge became
  the spawned runtime this post opened with — and with the edge in
  place (payload 1.16.9-8), the managed acceptance went green.
. **The loader half.** The first *standalone* package carrying the new
  edge died differently: `spawn 'java': no cached runtime satisfies
  engine 'java'`. Spec 30 splits resolution between dispatch and spawn,
  and the shim had its half — but the *bootstrap*, the loader inside
  every self-contained package, had none: it never installed the
  depended OpenJDK runtime and never exported the spawn lock. The fix
  shipped in tebako v2.1.6/v2.1.7 (#519/#521): the bootstrap now
  resolves every spawned edge at dispatch — cache hit by recorded
  digest, carried staging, or a fetch replaying the press-locked
  coordinates — and the package learned its four-slot layout
  (application, java executable, java image, inkscape), the spawned
  slots claimed by the lock and never mounted.
. **The shell form.** With dispatch fixed, the legs still died on the
  same jar — one layer down, inside the interpreter. Ruby's jing
  bindings do not spawn `java` the array way; they shell out through
  backticks, one quoted command string — and the spawn hook's original
  plan head (ruby#106) covered array-form spawns only. On POSIX the
  command string passed through verbatim, so the child JVM booted on
  its own environment image while the jar argument still spelled a path
  under the *parent's* mounts — a file the child cannot see. On Windows
  the bare `java` was never planned at all. The fix is the shell-form
  argument bridging described above (ruby#107, ruby source v0.2.33,
  spec'd in spec 22 §3.1): operands materialize parent-side, and on
  Windows the first-token command plans inline. No driver change was
  needed — the plan FFI had shipped in v2.1.0; the gap was in the
  interpreter's spawn hook, and that is where it was fixed.

The runtime line carrying that fix — 0.16.20 — is publishing as this
series is written; the packed-mn acceptance re-run that closes the arc
is the publish-time check.

== Inkscape: a mounted payload, on purpose

// source: _posts/2026-07-31-tebako-v2-hermetic.adoc (Inkscape 1.4.3 at
// /opt/inkscape); docs/spec/03 (DEPENDS mount composition); spec 11
// (longest-prefix dispatch).
Inkscape takes the other route: a payload slice *co-mounted* into the
process at `/opt/inkscape`. Mount composition is declarative and
versioned; nested mounts are allowed, duplicate mount points are a
named error, and any mount failure unwinds everything — never a
partial mount. Why co-mount here and spawn for java? Because the
consumer is different: the figure pipeline reads Inkscape's bytes in
the parent's own context, while mn2pdf must *be* a JVM process of its
own. The architecture has one answer per relationship, and the manifest
says which.

== Python: the honest host-tool route

// source: PROGRESS/25-command-exposure.md (the chocolatey python313 /
// xml2rfc version-range saga); PROGRESS/27-multi-runtime-ipc-future.md
// (xml2rfc as a host pip install today; the hermetic python payload as
// the direction); PROGRESS/00-INDEX.md 2026-09-01 (python/01 skeleton:
// CPython relocatable, zero patches).
xml2rfc — needed only for the IETF document flavors — is the one
component Metanorma still takes from the host. That is a deliberate,
declared choice, and an honest one:

* The distribution channels already carry it: the Chocolatey package
  depends on `python3` and installs xml2rfc as part of the install.
* The cost of the choice is on record: a chocolatey feed python3 bump
  (3.14.7) fell *outside* xml2rfc's declared range and broke installs
  until the dependency was pinned to a compatible line. Host tools mean
  host version drift; that is the trade-off, stated plainly.
* The exit is built: a python runtime is in the pipeline (the factory
  skeleton's relocation probe already returned *relocatable, zero
  patches* for CPython 3.12/3.13). When it ships, xml2rfc becomes a
  spec-30 spawned edge like java — hermetic, version-locked, and the
  host-python saga becomes a war story.

System versus payload is a per-tool decision, made tool by tool, in the
manifest — not a default the platform imposes.

== The platform story, honestly

// source: docs/spec/29 §3 (the visibility tiers: preload = POSIX
// default, exec-cache = the windows default and universal fallback);
// docs/spec/22 §3; PROGRESS/24 (the preload-inheritance asymmetry).
The mechanism differs per platform, and we would rather document the
asymmetry than hide it:

* **POSIX (macOS, Linux):** the preload shim — `LD_PRELOAD` /
  `DYLD_INSERT_LIBRARIES` plus the mount table — is *inherited* by
  spawned children, so a spawned tool's file reads resolve through the
  VFS. Dynamic interpreters (the JVM included) run with only their
  load-closure materialized; the env image stays mounted.
* **Windows:** there is no interposition API to inherit. The exec-cache
  tier is the answer: the interpreter's home tree materializes from the
  mounted image into a content-addressed cache, and the host copy runs.
  VFS-path arguments are materialized and rewritten at the spawn seam.
  The store artifact stays pristine either way — the cache is derived,
  content-keyed, disposable.

== Evidence

// source: PROGRESS/00-INDEX.md 2026-09-01 (the wrapper smoke:
// sidecar-verified pair, `TEBAKO_RUNTIME_IMAGE=rt.tfs
// ./rt --tebako-entry java -version` printed Temurin 21.0.12+8, exit 0);
// tebako-packages/openjdk release v2.1.5 (17 assets, 2026-09-02);
// tebako-packages/metanorma dogfood run 33731076066 (2026-09-03, green
// on payload 1.16.9-8, all legs); PROGRESS/13-benchmarks.md (the D0
// acceptance compile incl. the PDF leg spawning java/mn2pdf).
The java edge is not a rendering of a design doc. The published OpenJDK
runtime (Temurin 21.0.12; tebako line 2.1.5 at write time) was
smoke-verified against its own published bytes: download the pair,
check the SHA-256 sidecars, dispatch `--tebako-entry java -version`
through the wrapper — Temurin answers, exit 0. The feedstock's managed
dogfood — a real document compile driven through the store-resolved
payload — runs green on the 1.16.9-8 payload on every leg. And the
standalone acceptance suite's PDF legs are exactly the spawn path under
load: mn2pdf and jing running as dispatched children, fonts and all —
the arc above is the record of getting those legs to green.

Post 4 is about the other half of "nothing leaks": the jail — what
these composed processes may touch on your host, and who says so.
